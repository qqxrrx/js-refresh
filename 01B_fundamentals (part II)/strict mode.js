'use strict'

let hasDriversLicense = false
const passTest = true

if (passTest) {
  // without 'use strict' mode, this will be allowed
  hasDriverLicen = true
}

// without 'use strict' mode, this will be allowed

if (hasDriverLicen) {
  console.log(`can drive`)
}

// without 'use strict' mode, this will be allowed
// "interface" is a future reserved keyword!
const interface = 'X'
