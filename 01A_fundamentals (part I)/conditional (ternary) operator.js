const age = 19
age >= 18 ? console.log(`of full age`) : console.log(`still a child`)

const sta = age >= 18 ? `legal age` : `minor`
console.log(sta)

const ans = false
console.log(`the answer was: ${ans === false ? true : false}`)
